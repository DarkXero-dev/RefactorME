# dependencies
node_modules/

# build output
dist/
.astro/

# host adapters
.vercel/
.netlify/

# env / secrets
.env
.env.*
!.env.example

# os / editor
.DS_Store
*.log
push.sh

# private docs (scope-and-price note) and the script that generates them.
# Negated for public/ so a PDF ever served by the site stays tracked.
*.pdf
!public/**/*.pdf
make-scope-note.py
